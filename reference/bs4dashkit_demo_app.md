# Interactive bs4Dashkit demo app

Returns a richer runnable `shiny.appobj` for testing sidebar brand
states, hover-expand behavior, theme presets, navbar controls, and
footer styling in one place.

## Usage

``` r
bs4dashkit_demo_app()
```

## Value

A `shiny.appobj`.
