% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sidebar_mode.R
\name{use_dash_sidebar_brand_mode}
\alias{use_dash_sidebar_brand_mode}
\title{Set sidebar brand display mode for expanded and collapsed states}
\usage{
use_dash_sidebar_brand_mode(
  icon = NULL,
  collapsed = c("icon-only", "icon-text", "text-only"),
  expanded = c("icon-text", "icon-only", "text-only"),
  collapsed_text = NULL,
  expanded_text = NULL,
  collapsed_text_size = NULL,
  expanded_text_size = NULL,
  collapsed_text_weight = NULL,
  expanded_text_weight = NULL,
  debug = FALSE
)
}
\arguments{
\item{icon}{Font Awesome icon name used in the brand, e.g. "shield-halved".
Required when any mode includes "icon".}

\item{collapsed}{Display mode when sidebar is collapsed:
"icon-only", "icon-text", or "text-only"}

\item{expanded}{Display mode when sidebar is expanded:
"icon-text", "icon-only", or "text-only"}

\item{collapsed_text}{Short label for collapsed "icon-text" or "text-only" mode.
Keep <= 8 chars. NULL falls back to original DOM text (CSS truncates).}

\item{expanded_text}{Label for expanded "icon-text" or "text-only" mode.
Keep <= 30 chars. NULL falls back to original DOM text.}

\item{collapsed_text_size}{Optional CSS font-size applied to the sidebar brand
label while collapsed.}

\item{expanded_text_size}{Optional CSS font-size applied to the sidebar brand
label while expanded.}

\item{collapsed_text_weight}{Optional CSS font-weight applied to the sidebar
brand label while collapsed.}

\item{expanded_text_weight}{Optional CSS font-weight applied to the sidebar
brand label while expanded.}

\item{debug}{Logical. If \code{TRUE}, prints helpful messages to the browser console
for diagnosing missing icons or brand label elements.}
}
\value{
A \code{shiny.tag} or \code{shiny.tag.list} containing sidebar brand mode styling or dependencies.
}
\description{
Controls icon visibility, text visibility, and text content in both
collapsed and expanded sidebar states.
}
\details{
Modes:
\itemize{
\item "icon-only"  : icon shown, label hidden  — requires \code{icon}
\item "text-only"  : label shown, icon hidden  — \code{collapsed_text} / \code{expanded_text} optional
\item "icon-text"  : both shown               — requires \code{icon}; text args optional
}
}
